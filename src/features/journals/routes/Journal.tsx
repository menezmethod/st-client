import React from 'react';
import {useParams} from 'react-router-dom';

export const Journal = () => {
    const {id} = useParams();

    return (<></>
        // <ContentLayout title="JOURNAL TITLE">
        //     JOURNAL CONTENT
        // </ContentLayout>
    )
}
